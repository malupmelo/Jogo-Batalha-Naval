#pragma once

/* Inicializa o gerador de números aleatórios (semente). */
void aleatorio_inicializar(void);

/* Retorna um inteiro no intervalo [minimo, maximo] (inclusivo). */
int aleatorio_intervalo(int minimo, int maximo);
